import { Middleware } from 'koa';
import UserAgent from './lib/useragent';
export declare type UserAgentContext = {
    userAgent: UserAgent;
};
export declare const userAgent: Middleware;
export default userAgent;
