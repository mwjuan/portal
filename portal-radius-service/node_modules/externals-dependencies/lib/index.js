'use strict';

exports.__esModule = true;

exports.default = function () {
    var arr = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];

    var packageJson,
        externals = {};
    try {
        var packageJsonString = _fs2.default.readFileSync(_path2.default.join(process.cwd(), './package.json'), 'utf8');
        packageJson = JSON.parse(packageJsonString);
    } catch (e) {
        throw 'can not find package.json';
    }
    var sections = ['dependencies'].concat(arr);
    sections = new Set(sections);

    var deps = {};
    sections.forEach(function (section) {
        Object.keys(packageJson[section] || {}).forEach(function (dep) {
            externals[dep] = 'commonjs ' + dep;
        });
    });
    return externals;
};

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

module.exports = exports['default'];